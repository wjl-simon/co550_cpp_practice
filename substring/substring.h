#ifndef SUBSTRING_H
#define SUBSTRING_H

bool is_prefix(const char* str1, const char* str2);

int substring_position(const char* str1, const char* str2, int count = 0);

int substring_position2(const char* str1, const char* str2);

#endif
